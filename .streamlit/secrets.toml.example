ANTHROPIC_API_KEY = "sk-ant-REPLACE_ME"

# Cookie secret — any random string, keep it private
COOKIE_SECRET = "apollo-brain-change-this-secret-2026"

# Login credentials (auto-hashed by streamlit-authenticator on first run)
[credentials.usernames.admin]
name = "Admin"
email = ""
password = "admin"
